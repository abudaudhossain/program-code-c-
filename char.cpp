#include<iostream>
#include <bits/stdc++.h>
#include <string.h>

using namespace std;

int main()
{
    string s;
    cin >> s;
    int n =s.length();

    char char_array[n+1];

    strcpy(char_array,s.c_str());

    for(int i=0;i<n;i++){
        cout << char_array[i] <<" ";}
        cout <<endl;
   // cin >> s;
 char p[s.length()];

    int j;
    for(j=0;j<sizeof(p);j++){

        cout <<s[j]<<0;}
        cout <<"\n"<<sizeof(p);

    return 0;

}
